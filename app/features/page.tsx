import { Button } from "@nextui-org/button";

const page = () => {
    return (
        <div>
            <h1>Features Page</h1>
            <Button color="secondary" variant="flat">View featues</Button>
        </div>
    );
};

export default page;